{{ 'section-main-page.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
  @media screen and (min-width: 591px) {
        .section-header-wrapper {
            padding: 0 20px;
        }
    }
    .section-header-wrapper {
        max-width: 1220px;
        margin: 0 auto;
        {% comment %} padding: 0 10px; {% endcomment %}
        padding: 0 6rem;
    }
    header.section-header.wrapper-breadcrumb {
      border: none;
      background: #f7f7f7;
      padding: 15px 0;
      margin-bottom: 20px;
    }
    header.section-header.wrapper-breadcrumb h3 {
      font: 600 18px/1.15 Poppins;
      background: none;
      padding: 0;
      text-transform: uppercase;
      color: #151515;
      display: block;
      margin-bottom: 10px;
      margin-top: 5px;
      -webkit-font-smoothing: antialiased;
      -webkit-text-size-adjust: 100%;
    }
    
    .section-header-wrapper  a {
      text-decoration: none;
      color: #1c1d1d;
    }

    @media screen and (min-width: 769px) {
        .section-header {
            display: table;
            table-layout: fixed;
            width: 100%;
        }
    }
{%- endstyle -%} 

{% comment %} {{ page | json }}
{{ page.title }}
{{ page.content }} {% endcomment %}

<!-- If you want to see it as a console log in the browser, wrap it in a <script> tag -->
 <body>

 <script>
  // console.log({{ section | json }}); 
</script>

 </body>

<div class="section-{{ section.id }}-padding">
  <header class="section-header wrapper-breadcrumb">
  <div class="section-header-wrapper">
    <h3 class="title title--primary inline-richtext{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
      {{ page.title | escape }}
    </h3>

    <nav class="breadcrumb" role="navigation" aria-label="breadcrumbs">
      <a href="/" title="Back to the frontpage" previewlistener="true">Home</a>
        <span aria-hidden="true">›</span>
        <span>Your {{ page.title | escape }}</span>
    </nav>
  </div>
 {% comment %} <pre>{{ page | json }}</pre> {% endcomment %}
</header>

  <div class="rte{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
    {{ page.content }}
  </div>
</div>

{% schema %}
{ 
  "name": "t:sections.main-page.name",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ]
}
{% endschema %}
